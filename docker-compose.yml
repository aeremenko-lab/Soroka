services:
  bot:
    build: .
    container_name: soroka-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    command: python -m src.bot.main
